"use client";

// Deprecated: Redirects are now natively handled by Clerk routing to prevent loop issues
export function SignInClientRedirect({ targetUrl }: { targetUrl?: string }) {
  return null;
}

